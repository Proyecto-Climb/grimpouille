name: Checkout repository
uses: actions/checkout@v4
with:
  fetch-depth: 0

name: "Ruby on Rails CI: Rubocop"
on:
  workflow_dispatch:
  repository_dispatch:
    types: [rubocop_app]  # Triggered by the build workflow

jobs:
  rubocop:
    runs-on: ubuntu-latest
    steps:
      - name: Remove old cache
        run: rm -rf ~/.rubocop_cache
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Ruby and gems
        uses: ruby/setup-ruby@78c01b705fd9d5ad960d432d3a0cfa341d50e410 # v1.179.1
        with:
          bundler-cache: true
      - name: Lint Ruby files
        run: bundle exec rubocop --parallel --config .rubocop.yml
      - name: Trigger Build Workflow
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          event-type: build_app
      # - name: Migrate stuff
      #   run: bundle exec rubocop -a --only Migration/DepartmentName
      # - name: Generate binstubs
      #   run: bundle binstubs bundler-audit brakeman rubocop
      # - name: Security audit dependencies
      #   run: bin/bundler-audit --update
      # - name: Security audit application code
      #   run: bin/brakeman -q -w2
